╔══════════════════════════════════════════════════════════════════════════════╗
║                    COGNITO EMAIL TEMPLATE VARIABLE FLOW                      ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 1: User Registration Request                                           │
└─────────────────────────────────────────────────────────────────────────────┘
    
    POST /api/v1/auth/register
    {
        "email": "john@example.com",
        "password": "SecurePass123!",
        "name": "John Doe"
    }
    
    ↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 2: auth.py (Line 148-153)                                              │
└─────────────────────────────────────────────────────────────────────────────┘
    
    cognito_result = subtiter_cognito.register_user(
        email="john@example.com",     ← This email becomes {username}
        password="SecurePass123!",
        name="John Doe",
        subscription_plan="no_plan"
    )
    
    ↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 3: subtiter_cognito.py (Line 209-218)                                  │
└─────────────────────────────────────────────────────────────────────────────┘
    
    response = boto3_cognito_client.sign_up(
        ClientId="4ucvrbiupts4bs4bdt8mstedsm",
        Username="john@example.com",   ← {username} = "john@example.com"
        Password="SecurePass123!",
        UserAttributes=[
            {'Name': 'email', 'Value': 'john@example.com'},
            {'Name': 'name', 'Value': 'John Doe'},
            {'Name': 'custom:subscription_plan', 'Value': 'no_plan'}
        ]
    )
    
    ↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 4: AWS Cognito (Automatic Processing)                                  │
└─────────────────────────────────────────────────────────────────────────────┘
    
    AWS Cognito receives sign_up request:
    ✓ Username: "john@example.com"
    ✓ Generates verification code: "123456"
    ✓ Loads email template from User Pool
    ✓ Performs variable replacement:
        {username} → "john@example.com"
        {####}     → "123456"
    
    ↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 5: Email Template (BEFORE Cognito Processing)                          │
└─────────────────────────────────────────────────────────────────────────────┘
    
    <a href="https://subtiter.ai/api/v1/auth/confirm-signup/{username}/{####}">
        Verify Email Address
    </a>
    
    <div class="verification-code">{####}</div>
    
    ↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 6: Email Sent (AFTER Cognito Processing)                               │
└─────────────────────────────────────────────────────────────────────────────┘
    
    To: john@example.com
    Subject: Welcome to Subtiter.ai - Verify Your Email 🎬
    
    <a href="https://subtiter.ai/api/v1/auth/confirm-signup/john@example.com/123456">
        Verify Email Address
    </a>
    
    <div class="verification-code">123456</div>
    
    ↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 7: User Clicks Link                                                    │
└─────────────────────────────────────────────────────────────────────────────┘
    
    GET /api/v1/auth/confirm-signup/john@example.com/123456
    
    ↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 8: Confirmation Endpoint                                               │
└─────────────────────────────────────────────────────────────────────────────┘
    
    subtiter_cognito.confirm_user_signup(
        email="john@example.com",
        confirmation_code="123456"
    )
    
    ↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 9: AWS Cognito Confirms User                                           │
└─────────────────────────────────────────────────────────────────────────────┘
    
    boto3_cognito_client.confirm_sign_up(
        ClientId="4ucvrbiupts4bs4bdt8mstedsm",
        Username="john@example.com",
        ConfirmationCode="123456"
    )
    
    ✅ User is now verified and can log in!


╔══════════════════════════════════════════════════════════════════════════════╗
║                              KEY POINTS                                      ║
╚══════════════════════════════════════════════════════════════════════════════╝

1. {username} = The "Username" parameter in sign_up() call
   In our case: Username = email address

2. {####} = Auto-generated 6-digit verification code by AWS Cognito

3. Variable replacement happens AUTOMATICALLY by AWS Cognito
   We don't need to manually replace these variables

4. The email template is stored in AWS Cognito User Pool configuration
   Updated via: ./scripts/update_template.sh

5. Variables are case-sensitive: {username} and {####} (lowercase with braces)

