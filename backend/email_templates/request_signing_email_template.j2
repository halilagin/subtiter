<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            color: #333;
            padding: 20px;
        }
        .container {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            margin: 0 auto;
        }
        .header {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
            color: #007BFF;
        }
        .content {
            font-size: 16px;
            line-height: 1.5;
        }
        .button {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            font-size: 16px;
            color: #ffffff !important;
            font-weight: bold;
            background-color:rgb(0,0,0);
            text-decoration: none;
            border-radius: 5px;
        }
        .direct-link {
            margin-top: 20px;
            font-size: 12px;
            color: #777;
        }
        .klippers-team {
            margin-top: 20px;
            font-size: 16px;
            font-weight: bold;
            color: #777;
            margin-bottom: 40px;
            margin-top: 40px;
        }
        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #777;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">Document Signing Invitation</div>
        <div class="content">
            <!-- SVG Icon for Document Sign -->
            <svg xmlns="http://www.w3.org/2000/svg" width="50" height="50" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: middle; margin-right: 10px;">
                <path d="M16 2v6h6"></path>
                <path d="M8 2H4a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2v-8l-6-6z"></path>
                <path d="M16 13H9"></path>
                <path d="M16 17H9"></path>
                <path d="M10 9H9"></path>
            </svg>
            You are invited to sign the following document. Please click the button below to proceed:
            <br><br>
            <a href="{{ signing_callback_url }}?documentId={{ document_id }}&signerId={{ signer_id }}" class="button">Sign Document</a>
        </div>
        <div class="direct-link">
            If you cannot click the button above, please copy and paste the following link into your browser:
            <br><br>
            <a href="{{ signing_callback_url }}?documentId={{ document_id }}&signerId={{ signer_id }}">{{ signing_callback_url }}?documentId={{ document_id }}&signerId={{ signer_id }}</a>
        </div>
                    <div class="klippers-team">
                <p>Klippers Team</p>
            </div>
        <div class="footer">
            If you have any questions, please contact our support team.
            <br><br>
                            <a href="mailto:info@klippersai.com">info@klippersai.com</a>
        </div>
    </div>
</body>
</html>
