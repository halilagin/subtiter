You are invited to sign the following document:
{{ signing_callback_url }}?documentId={{ document_id }}




<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            color: #333;
            padding: 20px;
        }
        .container {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            margin: 0 auto;
        }
        .header {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
            color: #007BFF;
        }
        .content {
            font-size: 16px;
            line-height: 1.5;
        }
        .button {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            font-size: 16px;
            color: #ffffff !important;
            font-weight: bold;
            background-color:rgb(0,0,0);
            text-decoration: none;
            border-radius: 5px;
        }
        .direct-link {
            margin-top: 20px;
            font-size: 12px;
            color: #777;
        }
        .klippers-team {
            margin-top: 20px;
            font-size: 16px;
            font-weight: bold;
            color: #777;
            margin-bottom: 40px;
            margin-top: 40px;
        }
        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #777;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">View Signed Document</div>
        <div class="content">
            Please click the button below to view the signed document:
            <br><br>
            <a href="{{ signing_callback_url }}?documentId={{ document_id }}" class="button">View Document</a>
        </div>
        <div>
            If you cannot click the button above, please copy and paste the following link into your browser:
            <br><br>
            <a href="{{ signing_callback_url }}?documentId={{ document_id }}" class="button">{{ signing_callback_url }}?documentId={{ document_id }}</a>
        </div>
                    <div class="klippers-team">
                <p>Klippers Team</p>
            </div>
        <div class="footer">
            If you have any questions, please contact our support team.
            <br><br>
                            <a href="mailto:info@klippersai.com">info@klippersai.com</a>
        </div>
    </div>
</body>
</html>
